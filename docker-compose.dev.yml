services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: bndstr
      POSTGRES_USER: bndstr
      POSTGRES_PASSWORD: bndstr_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bndstr"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_dev_data:
