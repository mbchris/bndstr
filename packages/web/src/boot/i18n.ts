import { boot } from 'quasar/wrappers'

export default boot(() => {
  // Placeholder boot file for i18n wiring in Quasar mode.
  // Existing page-level translations are currently handled by app composables.
})
